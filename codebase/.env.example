# Điền khóa ở máy local. Không đưa file .env.local lên Git.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: stronger teach-back assessment; falls back to OPENAI_MODEL if unset.
OPENAI_LEARN_MODEL=gpt-4o
# Independent semantic evaluator.
EVAL_JUDGE_MODEL=gpt-4o
